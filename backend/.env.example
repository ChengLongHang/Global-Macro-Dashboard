# ---------------------------------------------------------------------------
# Already available per project owner -- required, no fallback data without them
# ---------------------------------------------------------------------------
FRED_API_KEY=f9eb9107ffbd44e9a0fa4198db7978cf
BANXICO_API_TOKEN=8096092eb2a524d61bcfa585fab4b73d85ddbab13c389a78ce615b90b75a662a

# ---------------------------------------------------------------------------
# Optional, free registration -- unlock direct central-bank coverage for
# South Korea and Turkey. Without these, those two countries fall back to
# World Bank/IMF (annual data) instead of central-bank monthly data.
#   ECOS (Bank of Korea):  https://ecos.bok.or.kr  (English site has a signup link)
#   EVDS (CBRT, Turkey):    https://evds2.tcmb.gov.tr
# ---------------------------------------------------------------------------
ECOS_API_KEY=
EVDS_API_KEY=xbMiGoA9xF

# ---------------------------------------------------------------------------
# No key required -- listed for completeness. Leave blank.
#   ECB SDW (Eurozone), Bank of Canada Valet, BCB SGS (Brazil),
#   World Bank API, IMF (imfp / legacy SDMX), Yahoo Finance
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# App config
# ---------------------------------------------------------------------------
DATABASE_URL=sqlite:///./macro_globe.db
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
LOG_LEVEL=INFO
